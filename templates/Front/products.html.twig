{% extends "front/base.html.twig" %}


{% block main %}


	{% for product in products %}
		<h2>{{product.name}}</h2>
		<h3>{{product.price}}</h3>
		<h3>{{product.stock}}</h3>
        <hr>
	{% endfor %}


{% endblock %}
